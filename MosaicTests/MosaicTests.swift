import Quick
import Nimble

class TableOfContentsSpec: QuickSpec {
    override func spec() {
        describe("test spec") {
            it("should pass") {
                expect(true).to(beTrue())
            }
        }
    }
}